[package]
name = "importer-engine"
version = "0.1.0"
edition = "2021"
description = "MemoryMesh Rust prototype for GPT export scanning/classification/extraction"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
