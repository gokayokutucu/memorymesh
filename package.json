{
  "name": "memorymesh-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @memorymesh/core && npm run build -w @memorymesh/runtime && npm run build -w @memorymesh/server && npm run build -w @memorymesh/cli",
    "test": "npm run test --workspaces --if-present",
    "docker:build": "docker compose build"
  }
}
